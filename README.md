for run test: 
fork repo on your lockal machine
you should have instoll:
        - noda letest version;
        -code editor (vscode or sth like that)
open terminal:        
run command npm init for install all nessessary dependencies;
for run tests use command npm run wdio
for get allure report use allure open

